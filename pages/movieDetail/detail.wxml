<view class="container">
    <video src="{{movie.src}}"></video>
    <view class="title">
        标题： {{movie.title}}
    </view>
    <view class="type">
        类型：{{movie.type}}
    </view>
    <view class="rate">
        评分： {{movie.rate}}
    </view>
    <view class="desc">
        剧情介绍：{{movie.desc}}
    </view>
</view>